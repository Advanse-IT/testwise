# Copy this to .env.local for local development
# In production, set this in Cloudflare Pages → Settings → Environment variables

# Your deployed Cloudflare Worker URL
VITE_CONTACT_API_URL=https://testwise-contact.YOUR_ACCOUNT.workers.dev/submit

# For local dev (while running 'wrangler dev' in the worker/ directory):
# VITE_CONTACT_API_URL=http://localhost:8787/submit
